name: images`non:`n  push: { branches: ["**"], paths: ["backend/**","frontend/**",".github/workflows/images.yml"] }`n  pull_request: { branches: ["main"], paths: ["backend/**","frontend/**",".github/workflows/images.yml"] }`n`njobs:`n  build-matrix:`n    runs-on: ubuntu-latest`n    strategy:`n      matrix:`n        service: [backend, frontend]`n    steps:`n      - uses: actions/checkout@v4`n      - uses: docker/setup-buildx-action@v3`n      - uses: docker/login-action@v3`n        with:`n          registry: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com`n          username: AWS`n          password: ${{ secrets.AWS_ECR_PASSWORD }}`n      - name: Build image`n        working-directory: ${{ matrix.service }}`n        run: |`n          docker build -t ${{ matrix.service }}:${{ github.sha }} .`n      - name: Save image as artifact (dry-run)`n        run: |`n          docker save ${{ matrix.service }}:${{ github.sha }} | gzip > ${{ matrix.service }}.tar.gz`n      - uses: actions/upload-artifact@v4`n        with:`n          name: ${{ matrix.service }}-image`n          path: ./*.tar.gz`n