name: iac-validate`non:`n  push: { branches: ["**"], paths: ["infra/terraform/**", ".github/workflows/iac-validate.yml"] }`n  pull_request: { branches: ["main"], paths: ["infra/terraform/**", ".github/workflows/iac-validate.yml"] }`n`njobs:`n  tf-validate:`n    runs-on: ubuntu-latest`n    defaults:`n      run:`n        working-directory: infra/terraform`n    steps:`n      - uses: actions/checkout@v4`n      - uses: hashicorp/setup-terraform@v3`n      - name: Terraform fmt check`n        run: terraform fmt -check -recursive`n      - name: Terraform init (no backend)`n        run: terraform init -backend=false`n      - name: Terraform validate`n        run: terraform validate`n